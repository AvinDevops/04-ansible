- name: default
  hosts: localhost
  tasks:
  - name: using default filter
    ansible.builtin.debug:
      msg: "Hi {{Name | default('Avinash')}}"

- name: uppercase
  hosts: localhost
  tasks:
  - name: using uppercase filter
    ansible.builtin.debug:
      msg: "{{Avinash | uppercase }}"